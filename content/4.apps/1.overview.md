---
title: Overview
description: What an Open Web Desktop application is and how it plugs into the core.
navigation:
  icon: i-lucide-info
---

An **OWD app** is a **publishable Nuxt module**. When listed in **`desktop.config.ts` → `apps`**, it registers **desktop programs**: launcher entries, window models, and commands. Apps do not replace the client — they plug into it.

## What an app provides

| Concept | Role |
|---------|------|
| **Nuxt module** (`defineNuxtModule`) | Adds components, plugins, Tailwind paths, optional `runtimeConfig`. |
| **Runtime plugin** | Client-only plugin named `owd-<slug>-register` calls **`defineDesktopApp(...)`** with the app configuration. |
| **`ApplicationConfig`** | Stable id, title, icons, **windows** (models), **entries** (menu items), **commands** (actions). |
| **Build** (`@nuxt/module-builder`) | Outputs `dist/module.mjs` consumed by the desktop like any other Nuxt module. |

## Reference repositories

| Repo | Why study it |
|------|----------------|
| [`app-about`](https://github.com/owdproject/app-about) | Minimal complete app — singleton window, `configKey`, playground launch plugin. |
| [`app-todo`](https://github.com/owdproject/app-todo) | Multi-window app, launch plugin pattern. |
| [`client` validate fixture](/internals/kernel-contract) | Smallest valid module checked by `desktop validate`. |

## What it should *not* do

- Do not depend on a specific theme (Win95, Nova, etc.): depend only on **`@owdproject/core`** as a peer and its public APIs.
- Do not assume fixed host URL routes: the app runs **inside** the desktop (windows, not necessarily global router paths).

## Learning path

Follow this order for a first app:

1. [Package layout](/apps/package-layout) — folder tree and `package.json` exports.
2. [Create from scratch](/apps/create-from-scratch) — end-to-end tutorial (recommended starting point).
3. [Module and runtime](/apps/module-and-runtime) — `module.ts`, `app.config.ts`, Tailwind.
4. [Plugins](/apps/plugins) — register plugin vs playground launch plugin.
5. [Windows and commands](/apps/windows-and-commands) — `ApplicationConfig` details.
6. [Playground](/apps/playground) — local dev mini-desktop.
7. [Build and publish](/apps/build-and-publish) — `prepack`, npm, validation.

Also see [desktop.config.ts](/setup/desktop-config) and [Package linking](/setup/package-linking).
