---
title: Create from scratch
description: End-to-end guide to building an OWD app module, from scaffold to desktop integration.
navigation:
  icon: i-lucide-list-checks
---

This walkthrough builds an app like **[`app-about`](https://github.com/owdproject/app-about)** — a singleton “About” window registered via **`defineDesktopApp`**. Use it as a checklist when starting a new repository.

## 1. Scaffold the repository

Create a Nuxt module package with the layout from [Package layout](/apps/package-layout):

```text
@owdproject/my-app/
├── package.json
├── src/
│   ├── module.ts
│   └── runtime/
│       ├── plugin.ts
│       ├── app.config.ts
│       └── components/Window/WindowMain.vue
└── playground/
    ├── nuxt.config.ts
    ├── desktop.config.ts
    └── package.json
```

**`package.json` essentials:**

- `"type": "module"`
- **`exports`** → `./dist/module.mjs`
- **`peerDependencies`**: `"@owdproject/core": "^3.4.0"`
- **`devDependencies`**: `nuxt`, `@nuxt/module-builder`, `@nuxt/schema`
- **Scripts**: `dev:prepare` (stub + playground prepare), `prepack` (release build)

Clone [`app-about`](https://github.com/owdproject/app-about) and rename if you prefer starting from a working tree.

## 2. Write `src/module.ts`

The module wires components, the register plugin, and Tailwind scanning:

```ts
import {
  defineNuxtModule,
  createResolver,
  addComponentsDir,
  addPlugin,
} from '@nuxt/kit'
import { registerTailwindPath } from '@owdproject/kit-primevue/kit/registerTailwindPath'

export default defineNuxtModule({
  meta: {
    name: 'owd-app-my-app',
    configKey: 'myApp', // optional — see step 3
  },
  async setup(_options, nuxt) {
    const { resolve } = createResolver(import.meta.url)

    addComponentsDir({ path: resolve('./runtime/components') })
    addPlugin(resolve('./runtime/plugin'))
    registerTailwindPath(nuxt, resolve('./runtime/components/**/*.{vue,mjs,ts}'))
  },
})
```

Optional **`configKey`** lets consumers override app-specific defaults from **`desktop.config.ts`** (About uses `configKey: 'about'` and merges into `runtimeConfig.public.desktop.about`).

## 3. Define `runtime/app.config.ts`

Export an **`ApplicationConfig`** object. From [`app-about`](https://github.com/owdproject/app-about/blob/main/src/runtime/app.config.ts):

```ts
export default {
  id: 'org.owdproject.about',
  title: 'About',
  category: 'system-tools',
  singleton: true,
  icon: 'mdi:hexagon-multiple-outline',
  windows: {
    main: {
      component: () => import('./components/Window/WindowAbout.vue'),
      resizable: false,
      size: { width: 448, height: 240 },
      position: { x: 400, y: 240, z: 0 },
    },
  },
  entries: {
    about: { command: 'about' },
  },
  commands: {
    about: (app) => {
      const existing = app.getFirstWindowByModel('main')
      if (existing) {
        existing.actions.setActive(true)
        existing.actions.bringToFront()
        return existing
      }
      return app.openWindow('main')
    },
  },
}
```

Details: [Windows and commands](/apps/windows-and-commands).

## 4. Add `runtime/plugin.ts`

The register plugin must run **client-side only** and use a stable name for **`dependsOn`** ordering:

```ts
import { defineNuxtPlugin } from 'nuxt/app'
import { defineDesktopApp } from '@owdproject/core'
import config from './app.config'

export default defineNuxtPlugin({
  name: 'owd-app-about-register', // owd-<slug>-register
  async setup() {
    if (import.meta.server) return
    await defineDesktopApp(config)
  },
})
```

Do **not** wrap `defineDesktopApp` in `app:created` — the current validator and reference apps call it directly in the plugin `setup`. See [Plugins](/apps/plugins).

## 5. Build the window component

Window components live under **`runtime/components/Window/`**. Keep them **theme-neutral**:

- Use **`DesktopWindowContent`** and core composables where possible.
- Avoid importing theme-specific chrome (taskbar, Win95 borders, etc.).

About’s window is a simple panel with props read from `runtimeConfig.public.desktop.about` when using **`configKey`**.

## 6. Configure the playground

**`playground/nuxt.config.ts`:**

```ts
export default defineNuxtConfig({
  modules: ['@owdproject/core'],
  ssr: false,
  compatibilityDate: 'latest',
})
```

**`playground/desktop.config.ts`:**

```ts
import { defineDesktopConfig } from '@owdproject/core'

export default defineDesktopConfig({
  theme: '@owdproject/theme-nova',
  apps: ['@owdproject/app-about'],
  modules: [],
})
```

**Critical rule:** every package listed in **`theme`**, **`apps`**, or **`modules`** must appear in **`playground/package.json`** `dependencies`. Mismatch causes install or runtime resolution errors.

**`playground/package.json` (minimal):**

```json
{
  "dependencies": {
    "@owdproject/core": "^3.4.0",
    "@owdproject/theme-nova": "^3.4.0",
    "@owdproject/app-about": "^3.4.0",
    "nuxt": "^4.4.4"
  }
}
```

When the theme imports PrimeVue directly (Nova explorer), also add **`primevue`** and **`@primeuix/themes`** — see [Playground](/apps/playground) and [Create a theme](/themes/create-from-scratch).

```bash
cd playground && pnpm install && pnpm dev
```

Or from the app root: `pnpm run dev` (runs `dev:prepare` then playground dev).

## 7. Optional: playground launch plugin

For dev ergonomics, auto-open your app after boot. Create **`playground/app/plugins/launch-about.client.ts`**:

```ts
import { nextTick } from 'vue'
import { defineNuxtPlugin } from 'nuxt/app'
import { useApplicationManager } from '@owdproject/core/runtime/composables/useApplicationManager'

export default defineNuxtPlugin({
  name: 'app-about-playground-launch',
  dependsOn: ['owd-app-about-register'],
  async setup(nuxtApp) {
    if (!import.meta.dev) return

    const applicationManager = useApplicationManager()

    nuxtApp.hook('app:mounted', async () => {
      await nextTick()
      for (let attempt = 0; attempt < 80; attempt++) {
        if (applicationManager.isAppDefined('org.owdproject.about')) {
          await applicationManager.execAppCommand('org.owdproject.about', 'about')
          return
        }
        await new Promise((r) => setTimeout(r, 50))
      }
    })
  },
})
```

Full reference: [`app-about` launch plugin](https://github.com/owdproject/app-about/blob/main/playground/app/plugins/launch-about.client.ts). See [Plugins](/apps/plugins).

## 8. Validate and wire into the desktop

From the app repo root:

```bash
desktop validate .
```

Checks include `src/runtime/plugin.ts`, `app.config.ts`, playground deps on your package and core, and optional launch plugin.

To test inside the **client monorepo**:

```json
// desktop/package.json
"@owdproject/app-about": "workspace:*"
```

```ts
// desktop/desktop.config.ts
apps: ['@owdproject/app-about']
```

```bash
pnpm install && pnpm run dev
```

## Next

- [Module and runtime](/apps/module-and-runtime) — deeper dive on `module.ts` and Tailwind.
- [Plugins](/apps/plugins) — register vs launch, validator rules.
- [Build and publish](/apps/build-and-publish) — npm release.
