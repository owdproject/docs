# Open Web Desktop — developer documentation

Documentation for **Open Web Desktop**: the client, `@owdproject/core`, kits, themes, and apps.

```bash
cd docs
pnpm install
pnpm dev
```

Content lives in `content/`. All pages are in **English**.
